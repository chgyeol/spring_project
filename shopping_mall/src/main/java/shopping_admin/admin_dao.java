package shopping_admin;

import java.util.ArrayList;

import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
public class admin_dao {
	int aidx;
	String aid,apass,aname,aemail,aphone,apart,aposition,ause,amaster,aindate;

	/*
	public ArrayList<Object> data(){
		ArrayList<Object> onedata = new ArrayList<Object>();
		onedata.add(this.getAidx());
		onedata.add(this.getAid());
		onedata.add(this.getApass());
		onedata.add(this.getAname());
		onedata.add(this.getAemail());
		onedata.add(this.getAphone());
		onedata.add(this.getApart());
		onedata.add(this.getAposition());
		onedata.add(this.getAuse());
		onedata.add(this.getAmaster());
		onedata.add(this.getAindate());
		return onedata;
	}
	*/
}
